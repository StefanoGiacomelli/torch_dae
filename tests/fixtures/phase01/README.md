# Phase 01 Synthetic Fixtures

This directory is reserved for Phase 01 synthetic fixtures. Files here must have no scientific
meaning and must not use real model names, repositories, checkpoints, papers, datasets, or metrics.

Current Phase 01 integration tests generate their full synthetic repositories under `tmp_path` so no
runtime environment, checkpoint, build output, or synthetic production card is committed.
