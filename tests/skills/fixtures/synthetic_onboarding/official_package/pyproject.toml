[project]
name = "synthetic-official-audio"
version = "1.2.3"
requires-python = ">=3.10,<3.12"
dependencies = [
  "torch==2.2.2",
  "torchaudio==2.2.2",
  "numpy==1.26.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
