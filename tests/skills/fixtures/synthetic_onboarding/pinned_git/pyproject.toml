[project]
name = "synthetic-pinned-git-audio"
version = "0.0.0"
requires-python = ">=3.10,<3.12"
dependencies = ["torch==2.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
