# Synthetic Ambiguous Embeddings Fixture

This repository is synthetic and scientifically meaningless. It exposes several plausible tensors so
the onboarding workflow must not silently select a default embedding.
