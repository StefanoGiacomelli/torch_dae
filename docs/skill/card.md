# Card mode

`card` produces one checkpoint-specific model card from validated evidence and workflow artifacts.
It does not combine incompatible variants or checkpoints. Officially reported facts, local
observations, inferences, unresolved items, and non-applicable fields remain distinguishable.

The card validator enforces lifecycle prerequisites and cross-document identity with its environment
and verification artifacts.
